# Gym Tracker

Minimal app used to test Citadel isolation + DB/storage APIs.

## MVP scope
- Writes a test row to its own DB
- Writes a test file to its own storage root
- Lists/reads back both

## Isolation
- DB file: `/data/apps/gym-tracker/db.sqlite`
- Storage root: `/data/apps/gym-tracker/`
