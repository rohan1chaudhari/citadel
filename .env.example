# Citadel host environment
# Copy to .env.local (host) or export in shell.

# Optional: required for voice/transcription features in some apps
OPENAI_API_KEY=
ELEVENLABS_API_KEY=

# Optional runtime toggles
NODE_ENV=development
